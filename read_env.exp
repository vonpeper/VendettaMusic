#!/usr/bin/expect -f
set timeout 60
spawn ssh -o PreferredAuthentications=password -o StrictHostKeyChecking=no panel-prosuite "cat /opt/vendetta/.env"
expect {
    "*assword:*" {
        send "Pp55202104#\r"
        exp_continue
    }
    eof
}
